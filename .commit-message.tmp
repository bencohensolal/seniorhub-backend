docs(agents): add guidance for writing large commit messages

Added clarification that large commit messages should be written incrementally in batches (e.g., 300 characters at a time) to avoid overwhelming the editor or tool.

This complements the existing rule that commit messages must always be written in a file using an editor, never directly on the command line with `-m`.
