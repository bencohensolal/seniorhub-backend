fix(migration): use Clerk user IDs instead of users table references

Migration 016 incorrectly referenced a `users` table that doesn't exist
in this project (uses Clerk for authentication). Changed `created_by` and
`revoked_by` columns from UUID foreign keys to VARCHAR(255) to store
Clerk user IDs directly.
